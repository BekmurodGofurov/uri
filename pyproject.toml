[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.pytest.ini_options]
testpaths = ["platform/tests"]
pythonpath = ["."]
addopts = "-v --cov=shared --cov=platform --cov-report=term-missing"
